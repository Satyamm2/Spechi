import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff5722",
      light: "#ff8a50",
      dark: "#c41c00",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
